<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">

    <title>Blog</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/css/bootstrap.min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.7/js/bootstrap.min.js"></script>
  </head>

  <body>
    <div class="top">
        <h1>--Travel blog--</h3>
    </div>
    <% if user_signed_in? %>
      <div class="home">
        <%= link_to root_path do%>
          <%= fa_icon("home", text: "Home", right: true) %>
        <% end %>
      </div>
      <div class="create">
        <i class="fa fa-comments fa-3x fa-fw header"> <%= current_user.name %> Welcome!</i>
        <%= link_to '新規作成', new_article_path, class: "create_btn btn btn-success" %>
        <%= link_to 'ログアウト', destroy_user_session_path, method: :delete, class: "create_btn btn btn-success" %>
      </div>
    <% else %>
      <div class="home">
        <%= link_to root_path do%>
          <%= fa_icon("home", text: "Home", right: true) %>
        <% end %>
      </div>
      <div class="create">
        <%= link_to '新規登録', new_user_registration_path, class: "create_btn btn btn-info" %>
        <%= link_to 'ログイン', new_user_session_path, class: "create_btn btn btn-info" %>        
      </div>
    <% end %>

    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>
    
    <%= yield %>
  </body>
</html>
